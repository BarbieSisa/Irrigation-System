<header class="fixed-filter">
    <div class="row mt-05">
        <div class="col-md-8">
            <h3>{{if this.model.facilityId 'Edit' 'Create'}} Facility</h3>
        </div>
        <div class="ml-50 col-md-1" style="display: flex;">
            <button type="button" class="btn btn-primary" {{action 'save'}}>Save</button>
            <button type="button" class="btn btn-primary ml-05" {{action 'cancel'}}>Cancel</button>
        </div>
    </div>
</header>

<section style="margin-top: 75px;">
    <div class="row">
        <CustomFormComponent 
        @columnSize={{4}} 
        @cssClass="mb-3 mt-3" 
        @max="45"
        @componentType="text" 
        @value={{this.facilityName}} 
        @placeHolder="Facility name" />
    </div>
</section>
