<header class="fixed-filter">
    <div class="row">
        <div class="col-md-9">
            <h3>Users</h3>
        </div>
        <div class="col-md-1">
            <button type="button" class="btn btn-primary mt-05" {{action 'createUser'}}>Create</button>
        </div>
    </div>
    <div class="row">
        <CustomFormComponent 
        @columnSize={{3}} 
        @cssClass="mb-3 mt-3" 
        @componentType="text" 
        @value={{this.searchText}} 
        @placeHolder="Search" 
        @onChange={{action 'makeRequestOnFieldChange'}}/>
    </div>
</header>

<section style="margin-top: 95px;">
    <div>
        <ul>
            {{#each this.loadedItems as |party index|}}
                <Users::ListItemComponent 
                @user={{party.user}} 
                @index={{index}} 
                @lastAvailableItemIndex={{this.lastAvailableItemIndex}}
                @onShouldLoadNextPage={{action 'loadNextPage'}}/>
            {{/each}}
        </ul>
        {{#if this.requestLoading}}
            <LoaderComponent/>
        {{/if}}
    </div>
</section>
